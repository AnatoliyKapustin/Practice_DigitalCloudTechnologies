<Page
    x:Class="DatMailReader.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DatMailReader.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Page.BottomAppBar>
        <CommandBar Height="Auto">
            <AppBarButton HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Icon="Delete"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid Background="#FFDDE7F3">
        <Pivot Title="WINMAIL.DAT VIEWER" Foreground="Gray">
            <PivotItem Header="Extract" Foreground="Black" Background="{x:Null}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Button VerticalAlignment="Top" HorizontalAlignment="Center" Content="extract new" FontSize="24" Background="#FF3BA3F9" Margin="10,10,10,0" Width="340" Height="85" BorderBrush="{x:Null}" Grid.Column="0" Grid.Row="0" Command="{Binding OpenFileCommand}"/>
                    <TextBlock Text="recently extracted" Grid.Row="1" Grid.Column="0" Margin="20,10,10,10" FontSize="18" Foreground="#FF3BA3F9"/>
                    <ListView ItemsSource="{Binding RecentlyExtractedFiles}" local:ItemClickCommand.Command="{Binding RecentlyOpenedMailClickCommand}" IsItemClickEnabled="True" Grid.Row="2" Grid.Column="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="200" Height="80" Orientation="Vertical" Background="#FF54B7FB">
                                    <TextBlock Padding="10,10,10,0" VerticalAlignment="Top" TextWrapping="NoWrap" Text="{Binding DisplayName}"/>
                                    <TextBlock Padding="10,10,10,0" VerticalAlignment="Bottom" TextWrapping="NoWrap" Text="{Binding From}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Attached">
                <Grid>
                <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ListView Grid.RowSpan="4" ItemsSource="{Binding RecentlyExtractedAtt}" IsItemClickEnabled="True" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Padding="10,10,10,10" VerticalAlignment="Top" TextWrapping="NoWrap" Text="{Binding DisplayName}"/>
                                    <TextBlock Padding="10,10,10,0" VerticalAlignment="Bottom" TextWrapping="NoWrap" Text="{Binding From}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
